pandas>=2.2.0
openpyxl>=3.1.0
scikit-learn>=1.4.0
joblib>=1.4.0
requests>=2.31.0
beautifulsoup4>=4.12.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gunicorn>=22.0.0
httpx>=0.27.0
python-multipart>=0.0.9
# Multi-agent runtime: supervisor + specialist worker agents orchestration.
langgraph>=0.2.0
# Pure-Python PDF text extraction. Fallback used when the poppler `pdftotext`
# binary is absent (e.g. Azure App Service / Oryx, which installs pip deps but
# no apt packages). Without this, scanned-or-digital PDFs collapse to the
# latin-1 junk fallback and extraction quality drops to ~20% in production.
pypdf>=4.2.0
